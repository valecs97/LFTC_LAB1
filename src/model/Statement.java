package model;

public enum Statement {
    READ,
    WRITE,
    ASSIGNEMNT,
    DECLARATIONSTART,
    DECLARATION,
    DECLARATIONEND,

    CONDITION,
    CONDITIONPARAMETER,
    ENDCONDITION,

    EXPRESSION,
    EXPRESSIONPARAMETER,
    ENDEXPRESSION,

    IF,
    ELSE,
    ENDIF,
    WHILE,
    ENDWHILE
}
