package model;

public enum Statement {
    READ,
    WRITE,
    ASSIGNEMNT,
    DECLARATION,

    CONDITION,
    CONDITIONPARAMETER,
    ENDCONDITION,

    EXPRESSION,
    EXPRESSIONPARAMETER,
    ENDEXPRESSION,

    IF,
    ELSE,
    ENDIF,
    WHILE,
    ENDWHILE
}
